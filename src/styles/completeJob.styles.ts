import { StyleSheet } from "react-native";


const CompleteJobStyles = StyleSheet.create({

    pickProductsContainer: {
        paddingHorizontal: 14,
        paddingTop: 10,
        display: 'flex',
        flexDirection: 'column',
        gap: 2,
    },

});

export default CompleteJobStyles